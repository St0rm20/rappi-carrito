<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/11.0.1"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.uniquindio.rappicarrito.controller.CarritoController"
            stylesheets="@../styles.css">

    <top>
        <HBox spacing="20" alignment="CENTER_LEFT" style="-fx-background-color: #f8f9fa; -fx-padding: 15; -fx-border-color: #ddd; -fx-border-width: 0 0 1 0;">
            <Button text="← Volver a Productos" onAction="#handleVolverALista"
                    style="-fx-background-color: #6c757d; -fx-text-fill: white; -fx-cursor: hand;"/>

            <Label text="Mi Carrito" maxWidth="Infinity" HBox.hgrow="ALWAYS"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #333;"/>

            <Button text="🗑 Vaciar Carrito" onAction="#handleVaciarCarrito"
                    style="-fx-background-color: #dc3545; -fx-text-fill: white; -fx-font-weight: bold; -fx-cursor: hand;"/>
        </HBox>
    </top>

    <center>
        <VBox spacing="10" style="-fx-padding: 20;">
            <HBox alignment="CENTER_LEFT">
                <Label text="Artículos seleccionados" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>
            </HBox>

            <ListView fx:id="itemsListView" VBox.vgrow="ALWAYS" style="-fx-background-color: transparent; -fx-border-color: #eee;">
                <placeholder>
                    <VBox alignment="CENTER" spacing="10">
                        <Label text="🛒" style="-fx-font-size: 40px;"/>
                        <Label text="Tu carrito está vacío" style="-fx-font-size: 18px; -fx-text-fill: #999;"/>
                    </VBox>
                </placeholder>
            </ListView>
        </VBox>
    </center>

    <bottom>
        <VBox spacing="15" style="-fx-background-color: white; -fx-border-color: #ddd; -fx-border-width: 1 0 0 0; -fx-padding: 20;">
            <HBox spacing="20" alignment="CENTER_RIGHT">
                <VBox alignment="CENTER_RIGHT">
                    <Label text="Total a Pagar:" style="-fx-font-size: 14px; -fx-text-fill: #666;"/>
                    <Label fx:id="totalLabel" text="\$ 0.00" style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #2e7d32;"/>
                </VBox>

                <Button fx:id="pagarButton" text="Confirmar Compra"
                        onAction="#handlePagar"
                        style="-fx-background-color: #ff6d00; -fx-text-fill: white; -fx-font-size: 16px; -fx-font-weight: bold; -fx-padding: 10 30; -fx-cursor: hand;"/>
            </HBox>

            <Label text="* Nota: Esta es una simulación académica."
                   style="-fx-font-size: 12px; -fx-text-fill: #999; -fx-alignment: center;" maxWidth="Infinity"/>
        </VBox>
    </bottom>

</BorderPane>